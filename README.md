# Quiz Solver


This project implements a Flask webhook that receives quiz tasks and solves them by loading the provided URL using a headless browser (Playwright), extracting data, computing answers, and posting submissions.


## Setup (local)


1. Clone the repo


```bash
git clone https://github.com/your-username/quiz-solver.git
cd quiz-solver
